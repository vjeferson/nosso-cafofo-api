import database from '../../database/connection';
import { INovoCliente } from '../../interfaces/novo-cliente';

export default class ClienteService {
    constructor() { };

    async create(dados: INovoCliente) {
        try {

        } catch (error) {
            throw error;
        }
    }

}